#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
ROM="${1:-$ROOT/build/game/n64game-gate3.z64}"
exec python3 "$ROOT/tools/n64game_build.py" validate-rom "$ROM"
