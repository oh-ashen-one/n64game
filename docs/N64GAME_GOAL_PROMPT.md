Create and pursue one persistent goal: deliver the complete public `oh-ashen-one/n64game` project exactly as specified by the authoritative master production prompt.

Before planning, delegating, editing, generating assets, creating the GitHub repository, or claiming status, read the following document completely from beginning to end:

`/Users/midir/Documents/New project 8/docs/N64GAME_MASTER_SPEC.md`

After the repository is published, the canonical public fallback is:

`https://raw.githubusercontent.com/oh-ashen-one/n64game/main/docs/N64GAME_MASTER_SPEC.md`

The local file is authoritative while working in the local checkout. The public fallback is for environments without that checkout. If neither copy can be accessed, stop and report the missing master document as a blocker. Do not invent, regenerate, reconstruct, summarize away, or replace its requirements.

Treat every section of the master prompt as part of the goal, including the genuine 18–25 minute playable opening, two complete 2v2 battles, two finished explorable destinations, world-map travel, companion-return sequence, saves/retries, original-IP restrictions, public repository, reproducible N64 build, anti-slop art contract, seven-stage asset reviews, polished loading presentation, the approved `INSERT CUTSCENE HERE` slot, and the complete GPT Image 4:3 storyboard package for the user’s later AI-video work.

Do not reduce the scope to a prototype, greybox, tech demo, partial vertical slice, or compiled ROM. Do not call placeholder-quality models, raw generated images, default materials, empty environments, stiff animation, or unfinished graphics acceptable “N64 style.” Quality takes priority over speed. Rebuild work that fails the master prompt’s visual, technical, legal, gameplay, performance, or evidence gates.

Work autonomously through research, specification maintenance, repository creation, toolchain setup, implementation, original asset production, integration, testing, optimization, storyboard generation, certification, and public release preparation. Use evidence rather than assumptions. Keep progressing while safe in-scope work remains. Report a blocker only after exhausting safe alternatives, and never silently weaken a requirement to get around one.

The goal is complete only when every item in the master prompt’s Final Acceptance Checklist has been verified. Read that checklist directly at each completion audit; do not rely on this shorter prompt as a substitute.
